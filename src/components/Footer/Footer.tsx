import { type ReactElement } from 'react';

const Footer = (): ReactElement => {
  return (
    <footer className="h-[60px] bg-[#c0c0c0] flex justify-center items-center font-black">
      <p>Работу выполнил Павел Терно</p>
    </footer>
  );
};

export default Footer;
