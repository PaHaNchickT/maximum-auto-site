import { type ReactElement } from 'react';

const Footer = (): ReactElement => {
  return (
    <footer className="h-[108px] bg-black flex justify-center items-center text-white px-[219px]">
      <p>Работу выполнил Павел Терно</p>
    </footer>
  );
};

export default Footer;
